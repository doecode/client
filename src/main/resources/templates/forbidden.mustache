{{> wrapper/header}}
<div class="row not-so-wide-row">
    <div class="col-md-3"></div>
    <div class="col-md-6 col-xs-12 static-content center-text">
        <h2 class="static-content-title">Forbidden</h2>
        {{#if has_message}}
        <p>{{message}}</p>
        {{else}}
        <p>You do not have appropriate permissions to view this content</p>
        {{/if}}
    </div>
    <div class="col-md-3"></div>
</div>
{{> wrapper/footer}}